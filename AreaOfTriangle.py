# WAP to find the area of a triangle
# Area = 1/2 (base * height [Perpendicular distance])

base = float(input('Enter the Base of a Triangle:    '))
height = float(input('Enter the Height of a Triangle:    '))
Area = 0.5 * base * height
print(f'The area of a triangle is {Area}')
